doctype html
html(lang="en")
	head
		title Flash Cards
	body
		include includes/header.pug
		if username
			h2 Te doy la bienvenida #{username}
			form(action="/bye-bye", method="post")
				button(type="submit") Adios
		block content
		include includes/footer.pug
		
